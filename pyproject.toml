[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "chargehub-berlin"
version = "0.1.0"
description = "ChargeHub Berlin - DDD + TDD + Streamlit"
requires-python = ">=3.10"
